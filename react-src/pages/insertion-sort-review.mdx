import Layout from "./post/layout.tsx"
import { ConfigProvider } from "../components/ConfigContext.tsx"
import InsertionSortCode from "../components/comics/insertion-sort/InsertionSortCode.tsx"
import InsertionSortSnapshot from "../components/comics/insertion-sort/InsertionSortSnapshot.tsx"

export const meta = {
  title: "Insertion Sort",
  date: "March 12, 2024",
  picturePaths: [
    "/graphics/insertion-sort/slideshow1/step7.png",
    "/graphics/insertion-sort/slideshow1/step8.png",
    "/graphics/insertion-sort/slideshow1/step9.png",
  ],
  category: "sorting-algs",
  description: "Sort a list of comparable elements.",
  runtime: "O(N^2)",
  pageType: "review",
}

# Insertion Sort

<InsertionSortSnapshot />

# Python Code

<InsertionSortCode />

export default ({ children }) => {
  return (
    <Layout meta={meta}>
      <ConfigProvider config={{ alignItems: "flex-start" }}>
        {children}
      </ConfigProvider>
    </Layout>
  )
}
